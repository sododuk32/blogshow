import { style } from '@vanilla-extract/css';
import { sprinkles, thumnailHeight, separateMediaQueres } from '@styles/theme/sparkles.css';
import { globals } from '@styles/theme/theme.css';

export const NewsLoaderStyle = style({
  width: '90%',
  display: 'inline',
});
